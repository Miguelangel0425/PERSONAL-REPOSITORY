{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar C++",
            "type": "shell",
            "command": "cmd.exe",
            "args": [
                "/c",
                "g++ -g ${file} -o ${workspaceFolder}\\${fileBasenameNoExtension}.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compilar archivo C++"
        }
    ]
}
